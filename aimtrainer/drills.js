export const ACCURATE_SPEED=.8;
export function accuracyState(player,dizzy=false){if(dizzy)return {ready:false,label:'DIZZY · AIM DISABLED',kind:'bad'};if(!player.grounded)return {ready:false,label:'AIRBORNE · INACCURATE',kind:'bad'};if(player.speed>ACCURATE_SPEED)return {ready:false,label:'MOVING · INACCURATE',kind:'bad'};return {ready:true,label:'STOPPED · READY TO SHOOT',kind:'ready'};}
export function newCounterDrill(){return {armed:false,travel:0,attempts:0,clean:0,delayTotal:0,stoppedAt:null,lastResult:'STRAFE LEFT OR RIGHT TO START'};}
export function stepCounterDrill(drill,player,lateralInput,dt,now){if(lateralInput&&player.grounded&&player.speed>ACCURATE_SPEED){drill.travel+=player.speed*dt;if(drill.travel>=.25){drill.armed=true;drill.stoppedAt=null;}}if(drill.armed){if(player.grounded&&player.speed<=ACCURATE_SPEED){drill.stoppedAt??=now;}else drill.stoppedAt=null;}return drill;}
export function scoreCounterShot(drill,player,dizzy,now){if(!drill.armed){drill.lastResult='STRAFE AGAIN FOR THE NEXT REP';return null;}drill.attempts++;const clean=accuracyState(player,dizzy).ready;if(clean){drill.clean++;drill.delayTotal+=Math.max(0,now-(drill.stoppedAt??now))*1000;drill.lastResult='CLEAN STOP';}else drill.lastResult=dizzy?'DIZZY · MISSED REP':'TOO EARLY · STOP BEFORE SHOOTING';drill.armed=false;drill.travel=0;drill.stoppedAt=null;return clean;}
